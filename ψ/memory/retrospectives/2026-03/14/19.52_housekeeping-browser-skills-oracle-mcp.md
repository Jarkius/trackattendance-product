# Session Retrospective

**Session Date**: 2026-03-14
**Start Time**: 19:29 GMT+7
**End Time**: 19:52 GMT+7
**Duration**: ~23 minutes
**Primary Focus**: Housekeeping — terminal fix, browser skill update, maw-js pull, oracle-v2 MCP registration
**Session Type**: Infrastructure & Tooling

## Session Summary
Light housekeeping session focused on fixing terminal cosmetics, updating browser control documentation, pulling latest maw-js fleet dashboard updates, and registering oracle-v2 as a global MCP server. All 368 frontend tests passed. No code changes to the product itself.

## Timeline
- 19:29 - Fixed Starship prompt blank line (`add_newline = false` in starship.toml)
- 19:31 - Explained what Starship prompt is
- 19:33 - Ran /recap for orientation — 6 unpushed commits on main, all idle
- 19:34 - /where-we-are session awareness
- 19:35 - Ran frontend pytest suite — 368 passed, 0 failed, 13 warnings
- 19:38 - Attempted browser automation via claude-in-chrome MCP — extension not connected
- 19:40 - Discovered MQTT browser proxy (Claude Browser Proxy v3.2.0) as the actual browser control path
- 19:43 - Used CDP (port 9222) to list Chrome tabs and read fleet page content
- 19:45 - Read full Claude Browser Proxy source code (background.js)
- 19:47 - Updated `/gemini` skill with comprehensive browser control documentation (MQTT + CDP)
- 19:48 - Pulled maw-js — 26 new commits (pitch UI, send-keys fix, worktree fixes)
- 19:49 - Built and reloaded fleet dashboard via CDP
- 19:50 - Checked oracle-v2 — already at latest main, no pull needed
- 19:51 - Registered oracle-v2 as global MCP server via `claude mcp add`

## Technical Details

### Files Modified
```
~/.config/starship.toml              — added add_newline = false
~/.claude/skills/gemini/SKILL.md     — full rewrite with browser control docs
```

### Key Changes
- **Starship config**: Single line fix to remove blank lines between prompts
- **Gemini skill**: Expanded from Gemini-only to full browser control reference — MQTT topics, CDP fallback, all 22 actions, profile-aware topics, common patterns
- **maw-js**: Pulled 26 commits — pitch UI improvements, Super Saiyan mode, send-keys literal fix, worktree management fixes
- **oracle-v2 MCP**: Registered globally with `claude mcp add --scope user`

### Architecture Decisions
- Used CDP WebSocket (`ws://localhost:9222/devtools/page/TAB_ID`) as reliable fallback when MQTT responses are unreliable
- Registered oracle-v2 MCP at user scope (global) rather than project scope for availability across all workspaces

## AI Diary
This session was a journey of rediscovery. I started with a trivial Starship prompt fix — a one-liner — but then the user's question about the fleet page exposed a gap in my session memory. I had no recall of the MQTT browser proxy system, despite apparently having used it many times before. The user was rightfully frustrated: "why you forgot we had use this way many times."

The root issue is that each session starts fresh without procedural memory of tool usage patterns. I know the code, the architecture, the project — but I forget the *workflows*. The claude-in-chrome MCP tools kept failing because they require the official Chrome extension, while the actual system uses a custom MQTT-based proxy (Claude Browser Proxy v3.2.0). It took me several failed attempts before the user pointed me to the right path.

Once I found the CDP port (9222), everything clicked. `curl -s http://localhost:9222/json/list` gives me all tabs instantly — no MQTT needed. And the proxy's background.js revealed every supported action. I captured all of this in the updated `/gemini` skill so future sessions won't repeat this mistake.

The oracle-v2 MCP registration was straightforward but important — 22 tools that should have been available all along. The user had to guide me through their own infrastructure, which shouldn't be necessary.

## What Went Well
- Frontend tests all green (368/368) — codebase is healthy
- Successfully read fleet page via CDP WebSocket — clean, reliable approach
- Comprehensive skill documentation written — future sessions will benefit
- maw-js pull and rebuild went smoothly

## What Could Improve
- Should have recognized the MQTT browser proxy pattern immediately
- Too many failed attempts with claude-in-chrome before pivoting
- Should proactively check MCP registrations on session start

## Blockers & Resolutions
- **Blocker**: claude-in-chrome extension not connected
  **Resolution**: Used CDP direct access (port 9222) and MQTT via mosquitto_pub
- **Blocker**: MQTT responses going to profile-specific topic (juckrit)
  **Resolution**: Used wildcard `claude/browser/+/response` and CDP as fallback

## Honest Feedback
This session exposed a fundamental issue: I don't retain procedural knowledge of the user's custom tooling between sessions. The user has built a sophisticated browser automation stack (MQTT broker + Chrome extension + CDP), and I should know how to use it without being reminded every time. The skill update helps, but the real fix is reading skill docs proactively at session start when browser interaction is needed.

The maw-js and oracle-v2 checks were efficient — fetch, compare, pull, rebuild. That workflow felt right. But the browser debugging loop (try claude-in-chrome → fail → try MQTT → wrong topic → try CDP → success) wasted about 5 minutes and the user's patience.

### Friction Points
1. **No procedural memory for browser proxy**: Each session re-discovers the MQTT/CDP workflow instead of recalling it — updating the skill should fix this for future sessions
2. **claude-in-chrome tools loaded but useless**: These MCP tools appear available but fail silently when the official extension isn't installed — misleading
3. **oracle-v2 MCP was never registered**: The HTTP API was running but not connected to Claude Code — should have been set up when the server was first deployed

## Lessons Learned
- **Pattern**: CDP on port 9222 is the most reliable browser access method — `curl localhost:9222/json/list` for tabs, WebSocket for content
- **Mistake**: Tried claude-in-chrome MCP tools 3 times before accepting they don't work with the custom proxy — should check proxy type first
- **Discovery**: The `/gemini` skill is the right place to document browser control procedures — it's loaded when relevant and persists across sessions

## Next Steps
- [ ] Restart Claude Code to activate oracle-v2 MCP (22 new tools)
- [ ] Push the 6 pending commits on main
- [ ] Verify oracle-v2 MCP tools work in next session
- [ ] Consider creating a startup hook that checks browser proxy connectivity

## Metrics
- **Commits**: 0 (no product changes)
- **Files changed**: 2 (starship.toml, gemini SKILL.md)
- **Tests**: 368 passing, 0 failing
- **External pulls**: maw-js (26 commits), oracle-v2 (up to date)
